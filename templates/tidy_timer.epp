<%- | String $service | -%>
[Unit]
Description=Timer to run the service to tidy PE metrics
Requires=<%= $service %>-tidy.service

[Timer]
OnCalendar=*-*-* 00:00:00
RandomizedDelaySec=10800
Persistent=true

[Install]
WantedBy=timers.target

